{
  "name": "pedagog-roditel-dialogue-coach",
  "version": "0.1.0",
  "private": true,
  "description": "Local prototype of a Russian dialogue coach for difficult relationship conversations.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "coverage": "node --experimental-test-coverage --test --test-coverage-include=src/dialogueAnalyzer.js tests/*.test.js",
    "serve": "python3 -m http.server 5173"
  }
}
